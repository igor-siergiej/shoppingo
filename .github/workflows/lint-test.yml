name: Lint & Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-test:
    uses: imapps/utils/.github/workflows/lint-test.yml@main
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
