import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#d8f7cd',
            contrastText: 'white'
        },
        secondary: {
            main: '#0c1ba8'
        }
    }
});

export default theme;
